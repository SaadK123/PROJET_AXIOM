import java.util.HashMap;

public class ObjectContainer {
    public static ObjectContainer Instance = new ObjectContainer();
    private HashMap<String,CommandLine> hash = new HashMap<>();

    // initiate
}
